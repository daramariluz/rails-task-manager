<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <i class="fas fa-check-square"></i><li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<i class="fas fa-square"></i>

<%= link_to "Add new task", new_tasks_path %>
